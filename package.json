{
  "name": "shoplite",
  "version": "1.0.0",
  "private": true,
  "description": "ShopLite monorepo root — manages dev tooling",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.3.0"
  },
  "lint-staged": {
    "backend/src/**/*.ts": [
      "eslint --fix"
    ],
    "frontend/src/**/*.{ts,tsx}": [
      "eslint --fix"
    ]
  }
}
